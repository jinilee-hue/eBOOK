# 진행 상황

`CLAUDE.md`의 「구현 순서」 기준.

| 단계 | 상태 | 비고 |
|---|---|---|
| 1. 프로토타입 분해 | 완료 | `prototype/moc-v6.html` 동작 보존. 리팩터만 |
| 2. 본문 연결 | 완료 | **14쪽** — 옛 1·2쪽, 옛 2·3쪽을 차례로 합쳤다. `js/content.js` |
| 3. 사전 확장 | 완료 | 70개 → **178개**. 죽은 키 0개 (전부 원문에 실재) |
| 4. 배경 씬 16개 | 완료 | `js/scenes.js`. `bg` 키 16개 ↔ 씬 16개 정확히 대응 |
| 5. Andika 자체 호스팅 | 완료 | SIL 7.000 woff2. OFL 고지문 포함 |
| 6. 검증 | **미완** | 기기 실측이 남았다. 아래 참조 |

## 확정 사항에서 바꾼 것 (사용자 지시)

`CLAUDE.md`의 「확정 사항 · 재논의 금지」 중 두 가지를 사용자 요청으로 바꿨다.

| 항목 | 확정값 | 현재 | 비고 |
|---|---|---|---|
| 패널 명암 | 밝은 패널 | **어두운 반투명 유리** | 확정 사유였던 "Andika 획이 가늘어 흐려짐"은 본문 굵기를 400→**500(Andika Medium)** 으로 올려 상쇄 |
| 문장 카드 | 상자 + 좌측 굵은 선 | **없음** | 누를 때만 옅게 반응 |
| 패널 폭 | 46% | **25%** (`--read-w`) | 배경 영상을 덜 가리도록 |
| 총 쪽수 | 16쪽 | **14쪽** | 영상 한 컷이 두 쪽 분량을 담는 경우가 있어 본문을 합침 |
| 읽어주기 | 본문 TTS 낭독 | **없음** | 지문 소리는 배경 mp4가 담당 |

밝은 패널은 `?dev=1` 검증 패널에서 그대로 비교해 볼 수 있다.

## 소리 구조 (바뀐 부분)

- **본문 낭독 TTS를 걷어냈다.** 지문 소리는 배경 mp4의 오디오 트랙이 낸다.
- `TTS` 모듈은 남아 있고 **모달의 발음 버튼**(낱말·문장)에만 쓰인다.
  Apple의 en-GB 기본 음성이 Daniel(남성)이라, 이름 점수제로 여성 음성을 고른다.
- 소리 있는 자동재생은 브라우저가 막는다. 그래서 처음엔 음소거로 깔고
  **게이트를 탭하는 순간**(사용자 제스처) 소리를 연다. 게이트를 없애면 소리도 못 켠다.
- 컨트롤 바의 `#play` 버튼은 이제 **영상 소리 스위치**(🔊/🔇)다.
  배경이 절차적 씬인 쪽에는 소리가 없으므로 버튼이 숨는다.
- 모달을 열면 영상 소리를 0.08로 눌렀다가(duck) 닫을 때 되돌린다.
- **`#go`(다 읽었어요)는 이제 처음부터 눌린다.** 낭독이 끝나는 시점이 사라졌기 때문이다.
  진행을 막는 관문은 "정답을 맞혀야 다음 쪽" 하나만 남았다.

> 지금 소리가 나는 쪽은 **1쪽뿐**이다. 나머지 14쪽은 mp4가 없어 절차적 씬 + 무음이다.

---

## 프로토타입에서 달라진 점

- **ES 모듈이 아니다.** `window.MOC` 전역에 싣는다.
  ES 모듈은 `file://`로 직접 열면 CORS로 막혀서, "브라우저에서 직접 열어 확인"이 불가능해진다.
- **`#dev` 검증 패널은 `?dev=1` 일 때만 붙는다.** 배포 빌드에는 흔적이 남지 않는다.
- **접근성 보강** — `#play` 40→44px, `:focus-visible` 링, 진행 점·읽기 구간 `aria-label`,
  단어 탭 영역 확대, 모달 포커스 복귀, 게이트 키보드 조작.
- **`#go` 라벨** — 프로토타입은 `wait` 상태에서도 "다 읽었어요"를 회색으로 보여줬다.
  SPEC의 표대로 `wait`면 "읽는 중…"이 뜨도록 `goLabel()`로 정리.
- **단어 단위 하이라이트를 시도한다.** 낱말마다 `.tok[data-ci]`(원문 문자 위치)를 심어 두고
  `onboundary`가 오면 짚는다. 모바일 Safari에서 이벤트가 안 오면 기존 문장 단위 그대로다.

## 1층 a·g — 확인 결과 (재논의 불필요)

`CLAUDE.md`는 "OpenType 기능으로 켤 수 있는지 확인하고 되면 켤 것"이라고 했다. 확인했고, **켜면 안 된다.**

- SIL Andika는 **기본값이 이미 1층 a·g** 다. `cmap: U+0061 → a.SngStory, U+0067 → g.SngBowl`
- `ss01`은 그것을 **2층으로 되돌리는** 기능이다. `GSUB ss01: a.SngStory → a, g.SngBowl → g`
- `ss02`는 이 폰트에 없다.

→ `font-feature-settings`를 쓰지 않는다. 근거는 `styles/fonts.css` 주석에 남겨 뒀다.

## 조판 자동 감사

체크리스트의 "16쪽 전부 스크롤 없이" / "18px 미만 없음"은 눈으로 세지 말고 돌려서 볼 것.

```
index.html?dev=1&audit=1
```

14쪽을 전부 조판해 보고 쪽마다 `구간 수 · 최소 폰트 · 통과 여부`를 우측 하단에 띄운다.
콘솔에서 `MOC.audit()` 로도 부를 수 있다. **창 크기를 바꿀 때마다 다시 돌려야 한다.**

## 남은 일

- [ ] 실제 태블릿 1194 / 1024 에서 감사 통과 확인 (가로·세로 양쪽)
- [ ] 모바일 Safari `speechSynthesis` 실기 확인 — 첫 재생이 게이트 터치 뒤인지
- [ ] 배경 mp4 — 현재 **`01-02_opening.mp4`** 하나뿐이고 1·2쪽이 함께 쓴다.
      (한 컷이 두 쪽에 걸쳐 있어서, 쪽을 넘겨도 영상을 되감지 않고 그대로 흘려보낸다)
      나머지 14쪽은 절차적 씬으로 나간다
      (`content/pages.js`의 `prompt` 필드가 생성용 영문 지문)

## 배경이 영상인지 절차적 씬인지 구분하기

영상을 못 읽으면 **사용자에게는 아무 표시 없이** 절차적 씬으로 폴백한다(설계상 의도).
그래서 "영상이 안 나온다"와 "절차적 씬이 잘 나온다"가 겉보기에 같다.
`?dev=1` 계측 패널 맨 아랫줄에 `배경 mp4 · 01_cottage.mp4` 또는
`배경 절차적 · mp4 실패(error)` 처럼 출처가 찍히니 그것으로 판별할 것.
