# Millions of Cats — 인터랙티브 eBook

Wanda Gág의 『Millions of Cats』(1928)를 배경 영상 위에서 읽는 영어 학습 eBook.
원문 출처는 Project Gutenberg #74181, 미국 내 퍼블릭 도메인이다.

**대상**: 초등 저·중학년 한국인 영어 학습자
**기기**: 태블릿 가로 (검증 해상도 1194×834 / 1180×820 / 1024×768). 특정 기기용 앱이 아니라
웹 페이지이므로 데스크톱 브라우저에서도 열린다. 조판은 화면 크기에 맞춰 다시 계산된다
**기준 프로토타입**: `prototype/moc-v6.html` — 이 파일의 동작이 정답이다. 갈아엎지 말고 확장할 것.

---

## 확정 사항 — 재논의 금지

디자인 리뷰를 거쳐 확정된 항목이다. 바꾸려면 먼저 물어볼 것.

| 항목 | 값 | 확정 이유 |
|---|---|---|
| 레이아웃 | L2 하단띠 | 좌측 배경 54% / 우측 본문 46% / 하단 전체폭 문제 띠 |
| 본문 서체 | **Andika 400** | SIL 문해 학습용 설계. 기하학적 서체보다 덜 딱딱함 |
| 패널 명암 | **밝은 패널** (`--paper #f7f3e9`) | Andika는 획이 가늘어 어두운 배경에서 흐려짐 |
| 배경 dim | 0.5 (밝은 패널 기준) | 패널이 밝으므로 배경까지 누를 이유 없음 |
| 본문 최소 크기 | **18px** | 태블릿을 1m 거리에서 볼 때의 하한. 절대 양보 금지 |
| 스크롤 | **전면 금지** | 한 화면에 전부. 넘치면 폰트가 아니라 쪽을 나눔 |
| 인터랙션 색 | `#0066FF` | 진행·정답·선택에만. 장식으로 쓰지 말 것 |
| 총 쪽수 | 11쪽 | `content/pages.js` 참조 |
| 행간 | **1.52** | 11쪽 전부 한 화면에 들어가는 값. 6쪽(489자)이 한계선이다 |
| 문장 간격 | **0.52em** | 한계는 0.56em (1024×768에서 6쪽이 쪼개짐). 한 단계 여유를 남긴 값 |

---

## 절대 하지 말 것

- **Inter / Roboto / Arial / 시스템 폰트 기본값** — 레퍼런스 CSS에 `--font-en: Inter`가 있으나 폐기됨
- **보라색 그라디언트**, 근거 없이 새로 만든 색
- **세로 스크롤** — `overflow:hidden`을 풀지 말 것
- **18px 미만 본문** — 넘치면 `buildBeats()`로 쪽을 나눈다
- **장식용 SVG 아이콘** — 정보를 담은 것만
- **곳곳에 흩어진 마이크로인터랙션** — 잘 설계된 전환 하나가 낫다
- **가짜 통계·인용구로 빈 공간 채우기**
- SVG로 사람·고양이를 직접 그리기 — 배경은 절차적 애니메이션 또는 mp4

---

## 파일 구조

```
index.html              앱 셸
styles/tokens.css       색·간격·반경·그림자
styles/reader.css       레이아웃, 본문, 문제 띠, 모달
js/content.js           content/pages.js를 여기로 (원문 + 번역 + 문제)
js/dict.js              클릭 사전
js/fit.js               폰트 자동 맞춤 + 쪽 자동 분할
js/scenes.js            배경 절차적 씬 16개 + mp4 폴백
js/tts.js               읽어주기
js/app.js               상태 기계, 렌더, 이벤트
assets/fonts/           SIL Andika 원본 (자체 호스팅)
assets/videos/          mp4 (선택. 없으면 절차적 씬으로 폴백)
```

---

## 소리 — 브라우저 내장 음성

낭독·단어·문장 모두 브라우저 `speechSynthesis` 로 나간다.
`js/tts.js` 가 이름으로 여성 음성을 고르고 `localStorage` 에 고정한다.
남성·장난 음성이 자동으로 잡히면 버리고 다시 고른다.

외부 TTS(OpenAI `gpt-4o-mini-tts`)를 붙여 봤으나 **물렸다.**

- 낭독만 mp3고 단어·문장은 내장 음성이라 **목소리가 둘**로 들렸다
  (사전 178개·문장 64개를 전부 뽑지 않는 한 피할 수 없다)
- 재생 레벨이 영상과 안 맞아 작게 들리고, 키우면 깨졌다

**다시 시도하지 말 것.** 하려면 사전·문장을 남김없이 뽑아 놓고 시작해야 한다.

영상 볼륨은 낭독·발음 중 0.08까지 내려가되 **0.6초에 걸쳐 서서히** 움직인다
(`js/scenes.js` 의 `rampDuck`). 계단처럼 끊으면 전환이 그대로 귀에 들린다.

---

## 구현 순서

1. **`prototype/moc-v6.html`을 위 구조로 분해.** 동작을 먼저 그대로 보존하고 리팩터만 한다.
2. **`content/pages.js` 연결** — 3쪽 샘플을 16쪽 전체로 교체.
3. **사전 확장** — 현재 약 70개. 원문에 나오는 초등 수준 밖 어휘를 훑어 200개 내외로.
4. **배경 씬 16개** — `js/scenes.js`. 아래 별도 항목 참조.
5. **Andika 자체 호스팅** — 아래 별도 항목 참조.
6. **검증** — 아래 체크리스트.

---

## 배경 씬 — 하이브리드 방식

`content/pages.js`의 각 쪽에 `bg`(씬 키), `video`(mp4 파일명), `prompt`(AI 영상 생성용 영문 프롬프트)가 들어 있다.

```js
// 우선순위: assets/videos/{video} 가 있으면 재생 → 실패하면 절차적 씬
```

레퍼런스 `BackgroundManager`(jinilee-hue/LIO)와 같은 패턴이다. 절차적 씬은 Canvas 2D로 만든다. 조건:

- **실루엣과 색면만.** 고양이 얼굴을 그리려 하지 말 것. 언덕을 채우는 검은 형체면 충분하다
- 60fps 불필요. 30fps로 충분하고 배터리에 낫다
- `prefers-reduced-motion` 존중 — 정지 프레임으로
- 쪽 전환 시 크로스페이드 0.8s

가장 중요한 씬은 4쪽(고양이 언덕)과 13쪽(마당의 고양이 떼)이다. 이 둘은 배경이 주인공이므로 먼저 만들 것.

---

## Andika 자체 호스팅

구글 폰트의 Andika는 SIL 원본을 간소화한 버전이라 일부 문자와 문자 동작이 빠져 있을 수 있다 (SIL 공식 안내). Andika는 OFL 라이선스이므로 원본을 배포에 포함할 수 있다.

- 원본: `https://software.sil.org/andika/` 또는 `github.com/silnrsi/font-andika`
- `assets/fonts/`에 넣고 `@font-face`로 로드
- 원본에는 **1층 `a`·`g` 변형**이 OpenType 기능으로 들어 있다. 활성화하면 아이가 손으로 쓰는 글자 모양과 같아진다. `font-feature-settings`로 켤 수 있는지 실제로 확인하고, 되면 켤 것
- OFL 고지문을 저장소에 포함

---

## 검증 체크리스트

기능:
- [ ] 16쪽 전부 스크롤 없이 한 화면에 들어간다
- [ ] 본문이 18px 미만으로 내려가는 쪽이 하나도 없다
- [ ] 긴 쪽(13, 15쪽)이 읽기 구간으로 자동 분할된다
- [ ] 다 읽어야 문제가 나온다. 정답이어야 다음 쪽으로 넘어간다
- [ ] 책을 접어도 읽어주기와 문제 흐름이 끊기지 않는다
- [ ] 접힌 상태에서 쪽을 넘겨도 접힘이 유지된다
- [ ] 단어·문장 클릭 시 밝은 모달. 단어장 저장이 본문에 반영된다
- [ ] 읽어주기 중 단어를 누르면 읽어주기가 멈추고 단어 발음만 나간다

기기:
- [ ] 태블릿 가로 1194 / 1024 양쪽에서 확인
- [ ] 세로로 돌렸을 때 깨지지 않는다 (본문 위 / 문제 아래)
- [ ] `speechSynthesis` 동작 확인. 첫 재생은 사용자 터치 이후여야 한다 — 소리 있는 자동재생은
      모든 최신 브라우저가 막는다. Safari 계열이 특히 엄격하다
- [ ] 터치 타깃 최소 44px

접근성:
- [ ] 키보드 포커스가 보인다
- [ ] 모달에 `role="dialog"` `aria-modal="true"`, Esc로 닫힘
- [ ] `prefers-reduced-motion`에서 배경 애니메이션과 전환이 멈춘다

---

## 알려진 제약

- **단어 단위 읽기 하이라이트**는 `SpeechSynthesisUtterance.onboundary`로 가능하나 모바일 Safari에서 이벤트가 오지 않는 경우가 많다. 현재는 문장 단위. 시도하되 반드시 문장 단위 폴백을 유지할 것
- `grid-template-columns` 트랜지션(접기/펴기)은 Safari 16+ 필요
- 접힌 상태에서는 본문 상자 높이가 0이라 폰트 맞춤 계산을 건너뛴다. 펼 때 트랜지션이 끝난 뒤 다시 계산해야 한다 (현재 470ms 지연)

---

## 저작권

원문은 미국 내 퍼블릭 도메인이다. Project Gutenberg 상표를 쓰지 않으려면 PG 참조를 모두 제거하면 되고, 남기려면 PG 라이선스 고지를 함께 넣어야 한다. 한국어 번역과 문제는 이 프로젝트의 창작물이다.
