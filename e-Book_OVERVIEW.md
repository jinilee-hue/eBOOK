# Millions of Cats — 인터랙티브 eBook 개요

Wanda Gág의 『Millions of Cats』(1928, 미국 내 퍼블릭 도메인)를 배경 영상 위에서 보고 듣는
영어 학습 eBook. 초등 저·중학년 한국인 학습자 대상.

주 사용 환경은 **태블릿 가로 화면**이다. 특정 기기에 맞춘 앱이 아니라 웹 표준만 쓴 페이지라
데스크톱 브라우저에서도 그대로 열리고, 화면 크기에 맞춰 스스로 조판을 다시 계산한다.

문제 풀이 없이 **영상이 처음부터 끝까지 끊기지 않고 이어지는** 감상형이다.
읽기보다 보고 듣기가 먼저다.

- 데모 — https://jinilee-hue.github.io/eBOOK/flow.html
- 저장소 — https://github.com/jinilee-hue/eBOOK

작성 기준: 2026-09-08 배포본 (`main` `0df4163` / `gh-pages` `f63a28e`)

---

## 1. 특징 및 주요 기능

### 한 줄 요약

**영상이 이야기를 나르고, 글은 필요할 때만 나온다.**
학습자가 버튼을 누르지 않아도 11쪽이 저절로 흘러간다.

### 기본 흐름

```
쪽이 열린다
  → 본문을 2.6초만 보여 주고 접는다   ← "글이 있다"는 것만 알린다
  → 영상 속 인물의 대사가 이야기를 끌고 간다
  → 대사가 없는 자리에서 장면 요약이 쉬운 영어로 짚어 준다
  → 영상이 끝나면 0.35초 쉬고 다음 쪽으로

언제든 책 버튼을 누르면
  → 본문이 펼쳐지고 영상 소리는 0.08까지 내려간다
  → 성우가 본문 전체를 읽고 낱말 하이라이트가 목소리를 따라간다
  → 다시 접으면 영상 소리로 돌아간다
```

넘기기 버튼이 없다. 학습자가 할 일은 **볼 것인가, 읽을 것인가**를 고르는 것뿐이다.

### 주요 기능

#### 소리 — 세 층이 겹치지 않게 짜여 있다

| 층 | 언제 나오나 | 무엇 |
|---|---|---|
| **영상 대사** | 기본 | 인물이 실제로 말하는 소리. 이야기의 기둥 |
| **장면 요약** | 대사가 없는 자리 | 쉬운 영어 한두 문장으로 장면을 설명 |
| **본문 낭독** | 책을 펼쳤을 때만 | 성우가 원문 전체를 읽는다 |

세 층이 절대 겹치지 않는다. 요약이 들어갈 자리는 영상의 대사 구간을 **실제로 측정해서**
미리 계산해 둔다 (`content/slots.js`). 대사가 영상의 70%를 넘는 쪽은 요약을 아예 뺀다.

영상 음량은 낭독·발음 중 0.08까지 내려가되 **0.6초에 걸쳐 서서히** 움직인다.
계단처럼 끊으면 전환이 그대로 귀에 들린다.

#### 낱말 단위 따라읽기

성우 음성을 Whisper로 분석해 **낱말마다 발음 시각**을 재 두었다.
하이라이트가 일정 속도로 흘러가는 것이 아니라, 성우가 실제로 그 낱말을 말하는 순간에 맞는다.

`SpeechSynthesisUtterance.onboundary` 는 모바일 Safari에서 이벤트가 오지 않는 경우가 많아
쓸 수 없다. 그래서 음성을 미리 만들고 시각을 측정하는 방식을 택했다.

#### 배경 영상

- 쪽마다 mp4 한 편, 총 12편
- 받는 동안에는 **첫 프레임 정지 이미지**가 자리를 지킨다 — 절차적 씬이 번쩍이지 않게
- 다음 쪽 영상과 음성을 미리 받아 둔다. 넘어갈 때 곧바로 나온다
- 영상을 못 읽으면 Canvas 2D 절차적 씬으로 자동 폴백
- 쪽 전환 크로스페이드 0.38s
- `prefers-reduced-motion` 이면 멈춘다

#### 어휘 학습

| 기능 | 내용 |
|---|---|
| 클릭 사전 | 낱말 178개. 한국어 뜻 + 그림 + 발음 |
| 내가 찾은 단어 | 눌러 본 낱말이 쌓이고 본문에도 표시된다 |
| 낱말 그림 | 178장. 뜻을 글자 없이 보여 준다 |
| 문장 발음 | 문장을 누르면 그 문장만 다시 들려준다 |

낭독 중에 낱말을 누르면 낭독이 멈추고 그 낱말 발음만 나간다.

#### 조작

버튼은 넷뿐이다. 읽는 중에는 흐려졌다가(투명도 0.28) 손을 대면 다시 진해진다.

| 버튼 | 하는 일 |
|---|---|
| 책 | 본문 펼치기 / 접기 (지니 효과로 버튼에 빨려 들어간다) |
| 속도 | 1× / 1.25× / 1.5× — 낭독·요약·영상이 함께 움직인다 |
| 소리 | 세로 슬라이더. 기본값 가운데 |
| 단어장 | 내가 찾은 단어 |

#### 마지막 화면

11쪽이 끝나면 마지막 영상 위에 **THE END** 가 아래에서 떠오르며 나타나고,
**다시 보기** 버튼이 뒤이어 나온다.

### 화면 설계 — 확정 사항

| 항목 | 값 | 근거 |
|---|---|---|
| 레이아웃 | 좌측 배경 영상 54% / 우측 본문 46% | 본문이 접히면 영상이 전면 |
| 본문 서체 | **Andika 400** | SIL이 문해 학습용으로 설계 |
| 패널 바탕 | `#f7f3e9` (밝은 종이) | Andika는 획이 가늘어 어두운 배경에서 흐려진다 |
| 본문 최소 크기 | **18px** | 태블릿을 1m 거리에서 볼 때의 하한. 양보 없음 |
| 행간 / 문장 간격 | 1.52 / 0.52em | 11쪽 전부 한 화면에 들어가는 값 |
| 스크롤 | **없음** | 넘치면 글자가 아니라 쪽을 나눈다 |
| 인터랙션 색 | `#0066FF` | 진행·선택에만. 장식으로 쓰지 않는다 |

### 접근성

- 모달에 `role="dialog"` `aria-modal="true"`, Esc로 닫힘
- 키보드 포커스가 보인다. 터치 타깃 최소 44px
- `prefers-reduced-motion` 에서 배경 애니메이션과 전환이 멈춘다
- 본문 서체 Andika — 난독 학습자를 고려한 글자 모양

---

## 2. 상용 e-Library 플랫폼과의 공통점 및 차이점

> 비교 기준은 Raz-Kids·Epic!·myON 류 **레벨별 리더 플랫폼의 일반적 특성**이다.
> 특정 제품의 현재 사양을 단정하지 않는다.

### 공통점

| 항목 | 내용 |
|---|---|
| 대상 | 초등 영어 학습자 |
| 낭독 | 원어민 음성으로 본문을 읽어 준다 |
| 따라읽기 | 읽는 위치가 화면에 표시된다 |
| 어휘 지원 | 낱말을 누르면 뜻·그림·발음이 나온다 |
| 기기 | 태블릿 가로 화면 중심 |
| 조작 | 아이가 혼자 다룰 수 있는 단순한 버튼 |

### 차이점

| 항목 | 상용 플랫폼 일반 | 이 프로젝트 |
|---|---|---|
| **진행 방식** | 학습자가 페이지를 넘긴다 | **저절로 넘어간다.** 넘기기 버튼이 없다 |
| **배경** | 삽화 정지 이미지 | **쪽마다 움직이는 영상.** 배경이 이야기를 함께 나른다 |
| **소리의 주인공** | 본문 낭독 | **영상 속 인물의 대사.** 낭독은 펼쳤을 때만 |
| **장면 설명** | 없거나 본문 낭독뿐 | **대사 없는 구간에 요약 낭독을 얹는다.** 자리는 자동 계산 |
| **낭독 음성** | 미리 녹음한 성우 또는 기기 TTS | **생성형 TTS로 미리 렌더링.** 사람 녹음 없이 톤을 지정 |
| **따라읽기 정밀도** | 문장 단위, 또는 고정 속도 추정 | **낱말 단위.** 실제 발음 시각을 측정해 맞춘다 |
| **모국어 지원** | 대개 영어 전용 | **한국어 번역·뜻풀이가 함께** |
| **확인 문제** | 읽고 나면 퀴즈 | **없다.** 몰입을 끊지 않는 것이 이 판의 목적 |
| **조판** | 고정 레이아웃 또는 스크롤 | **스크롤 없음.** 화면에 맞춰 글자 크기·쪽 분할을 자동 계산 |
| **서체** | 일반 산세리프 | **Andika** — 문해 학습 전용 설계 |
| **구조** | 서버·계정·DB 기반 서비스 | **정적 파일.** 서버·로그인·빌드 없이 브라우저에서 바로 |
| **콘텐츠 범위** | 수백~수천 권 라이브러리 | **한 권 심화.** 플랫폼이 아니라 한 작품의 완성도 |

### 이 방식의 강점

- **몰입** — 영상·대사·요약·낭독이 한 흐름으로 이어져, 읽기 자료보다 애니메이션에 가깝다.
  버튼을 누를 일이 없어 어린 학습자도 흐름이 끊기지 않는다
- **제작 비용** — 성우 섭외·녹음 스튜디오 없이 음성을 만든다. 문장이 바뀌면 다시 뽑으면 된다
- **배포** — 정적 파일이라 GitHub Pages·사내 서버·USB 어디에나 올라간다. 서버 운영이 없다
- **듣기 부담이 낮다** — 어려운 원문을 못 알아들어도 장면 요약이 쉬운 영어로 받쳐 준다

### 이 방식의 한계 — 분명히 해 둘 것

- **콘텐츠 확장** — 한 권마다 영상 12편·음성 30여 개를 만들어야 한다.
  라이브러리로 키우려면 제작 파이프라인이 먼저다
- **학습 기록 없음** — 진행 상황이 브라우저 `localStorage` 에만 남는다.
  기기를 바꾸면 사라지고, 교사가 볼 수 없다
- **성취 확인 장치 없음** — 문제를 뺐으므로 이해 여부를 측정할 수단이 이 판에는 없다
- **레벨 체계 없음** — 한 권뿐이라 레벨 분류·추천이 없다
- **용량** — 한 권이 약 110MB (영상 84MB). 네트워크가 느린 환경에서는 초기 로딩이 부담이다

---

## 3. 개발 Spec

### 3.1 기술 스택

| 층 | 사용 기술 | 비고 |
|---|---|---|
| 마크업·스타일 | HTML5 / CSS3 (Custom Properties) | 프레임워크 없음 |
| 스크립트 | Vanilla JavaScript (ES5 문법) | **빌드 도구 없음**, 트랜스파일 없음 |
| 모듈 방식 | `window.MOC.*` 전역 | `file://` 이 ES 모듈·fetch를 막아 JSON 대신 `.js` 로 싣는다 |
| 배경 폴백 | Canvas 2D | 절차적 씬 |
| 음성 합성 | OpenAI `gpt-4o-mini-tts` | 빌드 타임에 mp3로 렌더링 |
| 음성 정렬 | OpenAI `whisper-1` | `timestamp_granularities[]=word` |
| 음성·영상 처리 | ffmpeg / ffprobe | loudnorm, speechnorm, silenceremove |
| 도구 스크립트 | Python 3 (표준 라이브러리만) | 외부 패키지 의존 없음 |
| 폰트 | SIL Andika (OFL) | woff2 자체 호스팅 |
| 형상 관리 | Git + Git LFS | mp4·mp3 는 LFS |
| 배포 | GitHub Pages | `gh-pages` 브랜치 (LFS 미지원이라 평문 파일) |

**런타임 의존성 0개.** CDN·번들러·패키지 매니저를 쓰지 않는다. 브라우저가 파일을 열면 그대로 돈다.

### 3.2 파일 구조

```
flow.html               감상판 진입점

js/flow.js              상태 기계·렌더·이벤트          1,770줄
js/scenes.js            배경 영상·절차적 씬·음량         918줄
js/content.js           원문 + 한국어 번역               294줄
js/tts.js               브라우저 내장 음성 (폴백)        333줄
js/dict.js              클릭 사전 178개                  213줄
js/voice.js             미리 만든 음성 재생기            178줄
js/fit.js               폰트 자동 맞춤·쪽 분할           146줄
js/align.js             낱말 발음 시각 (생성물)

content/pages.js        원문 원본
content/summary.js      장면 요약문 11개 + 수동 제외 목록
content/slots.js        요약을 넣을 자리 (생성물)

styles/tokens.css       색·간격·반경·그림자              109줄
styles/reader.css       레이아웃·본문·모달               717줄
styles/flow.css         감상판 덮어쓰기                  335줄
styles/fonts.css        Andika @font-face

assets/videos/          mp4 12편        84MB
assets/words/           낱말 그림 178장 17MB
assets/audio/           본문 낭독 + 정렬 데이터  6.2MB
assets/audio-sum/       장면 요약 낭독 11개      760KB
assets/posters/         영상 첫 프레임 12장      920KB
assets/fonts/           Andika 3종 + OFL 고지    928KB

tools/                  제작·검증 스크립트 (배포에는 필요 없다)
```

`styles/flow.css` 가 기본 리더에서 쓰지 않는 것을 감춘다 — 문제 띠(`#quiz`), 결과 버튼(`#resbtn`),
넘기기 버튼(`.go-wrap`), 별도 하이라이트 버튼(`#hl`, 책 버튼에 합쳤다).

### 3.3 대상 기기

| 항목 | 값 |
|---|---|
| 기기 | **태블릿 가로** (주 사용 환경). 데스크톱 브라우저에서도 동작 |
| 검증 해상도 | 1194×834 · 1180×820 · 1024×768 |
| 브라우저 | Safari 16+ / Chrome / Edge — 최근 2년 내 버전 |
| 세로 모드 | 깨지지 않는다 |
| 최소 터치 타깃 | 44px |

검증 해상도는 태블릿 가로에서 흔한 세 가지를 잡은 것이다. 레이아웃은 고정 크기가 아니라
화면에 맞춰 글자 크기와 쪽 분할을 다시 계산하므로, 이 셋이 아닌 해상도에서도 한 화면에 들어간다.

`grid-template-columns` 트랜지션(접기/펴기)에 Safari 16+ 가 필요하다.

### 3.4 동작 상수

| 항목 | 값 | 왜 |
|---|---|---|
| 쪽 사이 간격 | 350ms | 0이면 급해 보인다. 한 박자만 남긴다 |
| 본문 미리보기 | 2,600ms | 글이 있다는 것만 알리는 시간 |
| 접힘 / 펼침 | 220ms / 280ms | 접힐 때가 더 빨라야 빨려 들어가 보인다 |
| 배경 크로스페이드 | 380ms | |
| 낭독 중 영상 음량 | 0.08 | 0.6초에 걸쳐 서서히 |
| 배속 | 1× / 1.25× / 1.5× | 느리게는 두지 않는다 — 낭독이 이미 느리다 |
| 기본 음량 | 0.5 | `<audio>.volume` 천장이 1이라 위로 올릴 여지를 남긴다 |

### 3.5 조판 규칙 — 확정값

| 항목 | 값 |
|---|---|
| 본문 서체 | Andika 400 |
| 패널 바탕 | `#f7f3e9` |
| 배경 dim | 0.5 |
| 본문 최소 크기 | **18px** |
| 행간 | **1.52** |
| 문장 간격 | **0.52em** (한계 0.56em) |
| 인터랙션 색 | `#0066FF` |
| 스크롤 | 금지 (`overflow:hidden`) |

색은 전부 `styles/tokens.css` 의 CSS 변수로 관리한다. 인라인 hex를 쓰지 않는다.

### 3.6 음성 파이프라인

```
원문 (content/pages.js)
   ↓  tools/make_pages_audio.py
gpt-4o-mini-tts · voice=sage · speed=0.92 · instructions 로 톤 지정
   ↓  ffmpeg  speechnorm + loudnorm I=-12 TP=-1.0 LRA=7
assets/audio/pNN.mp3          쪽 단위 낭독
   ↓  tools/align_audio.py
whisper-1 · timestamp_granularities[]=word
   ↓
js/align.js                   낱말마다 시작·끝 시각
   ↓
따라읽기 하이라이트가 실제 발음에 맞는다
```

장면 요약도 같은 방식이되 `speed=0.94`, `loudnorm I=-14`, 끝의 묵음 제거를 더한다.
묵음이 붙어 있으면 요약이 실제보다 길어 보여 대사 사이 틈에 못 들어간다.

### 3.7 장면 요약 자리 계산

`tools/find_speech.py` 가 영상의 대사 구간을 측정해 `content/slots.js` 를 만든다.

| 상수 | 값 | 뜻 |
|---|---|---|
| `PAD` | 0.30초 | 대사 앞뒤로 비켜 주는 여백 |
| `DROP` | 0.70 | 말이 영상의 70%를 넘으면 요약을 아예 뺀다 |
| `SHORT` | 0.45 | 45%를 넘으면 짧게 쓰라고 권한다 |
| `WIN` / `OVL` | 6.0초 / 2.0초 | 인식 창 길이와 겹침 |

**whisper-1 의 두 가지 함정을 막아 두었다.**

1. **뒤쪽 누락** — 통째로 보내면 뒷부분을 흘린다. 같은 파일을 두 번 넣어도 결과가 다르다.
   → 겹치는 창으로 나눠 보내고 합친다
2. **환청** — 대사가 없는 구간에서 `"Thanks for watching"` 같은 문구를 지어낸다.
   배경 음악이 깔려 있어 음량으로는 갈리지 않는다 (환청 -27.3 dB / 진짜 대사 -24.4 dB).
   → ① 그 쪽 원문과 낱말을 대조해 절반도 안 맞으면 버리고
     ② 여러 창에서 두 번 이상 잡힌 구간만 인정한다

현재 요약이 나오는 쪽과 그 자리:

| 쪽 | 요약 자리 | 쪽 | 요약 자리 |
|---|---|---|---|
| 1 | 0.0초 | 7 | 없음 (대사 97%) |
| 2 | 0.0초 | 8 | 없음 (대사 88%) |
| 3 | 0.0초 | 9 | 6.5초 |
| 4 | 0.0초 | 10 | 없음 (대사 77%) |
| 5 | 0.0초 | 11 | 없음 (수동 제외) |
| 6 | 없음 (수동 제외) | | |

전부 영상이 끝나기 전에 끝난다. 영상을 되감아 반복하는 쪽이 하나도 없다.

9쪽은 자동 계산값 대신 손으로 정한 자리(6.5초)를 쓴다. `content/summary.js` 의
`SUMMARY_AT` 에 적으면 `slots.js` 의 자동 값보다 앞선다 — 자동 생성에 지워지지 않는다.

### 3.8 상태 저장

| 항목 | `localStorage` 키 |
|---|---|
| 음량 | `moc.vol` |
| 음량 기본값 판 번호 | `moc.vol.rule` |
| 선택한 음성 | (tts.js 내부) |

계정·서버 저장은 없다. 기기를 바꾸면 초기화된다.

### 3.9 검증

| 도구 | 하는 일 |
|---|---|
| `tools/smoke.sh` | 헤드리스 Chrome으로 실제 부팅 → 조판 감사 → 정의 검사 |
| `tools/check_defs.py` | 호출되지만 정의가 없는 함수를 잡는다 |
| `?dev=1&audit=1` | 앱 내장 조판 감사 — 넘침·18px 미만을 해상도별로 확인 |

문법 검사만으로는 지워진 함수를 못 잡는다. 그래서 실제로 실행해 본다.

**검증 항목**

- 11쪽 전부 스크롤 없이 한 화면 (1194 / 1024 양쪽)
- 18px 미만으로 내려가는 쪽 없음
- 영상 대사·장면 요약·본문 낭독이 서로 겹치지 않음
- 쪽이 넘어갈 때 앞뒤 영상 소리가 겹치지 않음
- 영상이 중간부터 시작하지 않음
- 책을 펼쳤다 접어도 따라읽기가 어긋나지 않음
- 낭독 중 낱말을 누르면 낭독이 멈추고 발음만 나감

### 3.10 배포

```
main        소스 + LFS로 관리하는 영상·음성
gh-pages    배포본. LFS가 아닌 평문 파일 (Pages는 LFS를 서빙하지 못한다)
```

배포에서 제외: `영상편집/`(Premiere 작업 파일), Project Gutenberg 원본 덤프, 음성 시청 샘플.

미디어 주소에도 판 번호(`?v=`)를 붙인다. 붙이지 않으면 영상을 갈아 끼워도
브라우저가 캐시의 옛 파일을 계속 쓴다.

### 3.11 알려진 제약

- **첫 재생은 터치 이후** — 모든 최신 브라우저가 소리 있는 자동재생을 막는다.
  시작 게이트가 그 한 번의 터치를 받는 역할을 한다 (Safari 계열이 특히 엄격하다)
- **접힌 상태의 조판** — 본문 높이가 0이라 폰트 계산을 건너뛴다.
  펼 때 트랜지션이 끝난 뒤 다시 계산한다 (470ms 지연)
- **초기 용량** — 한 권 110MB. 다음 쪽 영상을 미리 받아 두는 것으로 완화한다
- **학습 기록 미연동** — `localStorage` 뿐. 교사용 대시보드·계정 연동은 없다

### 3.12 저작권

- 원문은 미국 내 퍼블릭 도메인 (Project Gutenberg #74181, Wanda Gág, 1928)
- 한국어 번역·요약문은 이 프로젝트의 창작물
- Andika는 SIL OFL — 원본과 함께 `OFL.txt` · `FONTLOG.txt` 를 배포에 포함
- 배포본에 Project Gutenberg 상표·참조를 남기지 않는다

---

## 4. 활용 방법

### 4.1 수업에서

| 쓰임 | 어떻게 | 걸리는 시간 |
|---|---|---|
| **도입 감상** | 교실 대형 화면에 띄우고 처음부터 끝까지 함께 본다. 조작이 필요 없다 | 2분 13초 |
| **끊어 보기** | 쪽 이동 버튼(◀ ▶)으로 한 장면씩 멈춰 가며 이야기를 확인한다 | 자유 |
| **정독 전환** | 책 버튼을 눌러 본문을 펼치면 성우 낭독과 낱말 하이라이트가 시작된다 | 쪽당 20~40초 |
| **쉐도잉** | 펼친 상태에서 문장을 눌러 그 문장만 반복 재생. 따라 말하게 한다 | 자유 |
| **2차 청취** | 속도를 1.25×나 1.5×로 올려 다시 듣는다. 귀가 트인 뒤의 확인용 | 1분 47초 (1.25×) |

**넘기기 버튼이 없다는 점이 수업에서 유리하다.** 교사가 진행을 조작하지 않아도 흘러가므로,
아이들의 반응을 보거나 판서를 하는 동안에도 화면이 멈추지 않는다.
멈추고 싶을 때만 쪽 이동 버튼을 쓰면 된다.

### 4.2 학습자 개별 학습

- **혼자 보기** — 태블릿을 주고 그대로 두면 된다. 설명이 필요한 조작이 없다
- **모르는 낱말** — 본문을 펼친 상태에서 낱말을 누르면 한국어 뜻·그림·발음이 나온다
- **다시 보기** — 마지막 화면의 버튼으로 처음부터 되돌아간다
- **가정 학습** — 링크만 보내면 된다. 앱 설치도 로그인도 없다

읽기 부담이 큰 학습자는 **책을 접은 채로만 봐도 이야기가 전달된다.**
영상 대사와 장면 요약이 내용을 나르기 때문이다. 준비가 되면 그때 책을 펼치면 된다.

### 4.3 어휘 지도

- 학습자가 눌러 본 낱말이 **"내가 찾은 단어"** 에 쌓이고, 본문에도 표시가 남는다
- 단어장 화면에서 **인쇄** 버튼을 누르면 그날 찾은 낱말을 종이로 뽑을 수 있다
  (`@media print` 규칙이 화면 요소를 걷어내고 낱말만 남긴다)
- 어느 낱말을 눌렀는지가 곧 **그 학습자가 모르는 낱말**이므로, 개별 지도의 실마리가 된다

수록 어휘는 178개다. 원문에 나오는 초등 수준 밖 낱말을 훑어 뽑았다.

### 4.4 배포 방법

정적 파일이라 서버 없이도 돌아간다. 상황에 맞는 것을 고르면 된다.

| 방법 | 준비 | 적합한 상황 |
|---|---|---|
| **웹 링크** | 없음 (이미 배포됨) | 가정 학습, 원격 수업, 링크 공유 |
| **사내 서버** | 파일 복사 | 외부 접속을 막는 환경, 사내 전용 |
| **USB·로컬 폴더** | 파일 복사 | 인터넷이 없는 교실. `flow.html` 을 브라우저로 열면 된다 |

용량은 한 권 약 110MB(영상 84MB)다. 교실 무선망이 느리면 **미리 한 번 열어 두면**
브라우저 캐시에 남아 두 번째부터는 곧바로 뜬다. USB 배포도 같은 이유에서 유효하다.

### 4.5 이럴 때는 맞지 않는다

솔직하게 적어 둔다. 무리해서 쓰면 효과가 없다.

- **진도 관리가 목적일 때** — 누가 어디까지 봤는지 남지 않는다. 기록은 기기 안에만 있다
- **성취 측정이 목적일 때** — 이 판에는 확인 문제가 없다. 이해했는지 알 수 없다
- **레벨별 배정이 목적일 때** — 한 권뿐이라 레벨 체계가 없다
- **긴 수업 시간을 채워야 할 때** — 전체가 약 2분이다. 감상 뒤 활동은 따로 설계해야 한다

### 4.6 다음 단계로 검토할 것

지금은 한 권을 깊게 만든 프로토타입이다. 넓히는 길은 크게 둘이다.

| 방향 | 필요한 일 | 얻는 것 |
|---|---|---|
| **권수 확대** | 영상·음성 제작 파이프라인을 세운다 (한 권당 영상 12편·음성 30여 개) | 라이브러리로서의 가치 |
| **학습 관리 연동** | 진행·어휘 기록을 서버에 보내고 교사 화면을 만든다 | 수업 운영 도구로서의 가치 |

두 방향은 성격이 다르다. 어느 쪽을 먼저 볼지 정하는 것이 다음 결정이다.
